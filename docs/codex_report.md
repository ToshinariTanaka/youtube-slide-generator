## 今回やったこと

- `図表：` の文章を `graph`、`comparison`、`flow`、`relation`、`card` の汎用図解タイプに分類する処理を追加しました。
- 一次関数・右上がり・右下がりなどを `graph` の一例として扱い、比較・流れ・関係系のキーワードを優先して判定するようにしました。
- 各タイプごとに簡易SVGまたはカード表示を切り替える図解プレビューを追加しました。
- READMEに図表タイプ別の入力例と判定ルールを追記しました。
- プロジェクト状況とアーキテクチャ説明を、汎用図解タイプ方式に合わせて更新しました。

## 変更ファイル

- `script.js`
- `style.css`
- `README.md`
- `docs/architecture.md`
- `docs/project_status.md`
- `docs/codex_report.md`

## テスト結果

- `node --check script.js` を実行し、構文エラーがないことを確認しました。
- `git diff --check` を実行し、空白エラーがないことを確認しました。
- `node - <<'NODE' ... require('playwright') ... NODE` でスクリーンショット取得を試みましたが、環境にPlaywrightがないため取得できませんでした。

## 注意点

- 図表生成は引き続きキーワードベースの簡易表示です。自由入力から高度な図解を生成するAI連携は未実装です。
- UI表示の変更を含みますが、この環境にはPlaywrightやブラウザ実行環境がなく、スクリーンショット確認は未実施です。

## 次にやるべきこと

- 実ブラウザで5タイプの図表入力例をそれぞれ生成し、見た目とPNG保存を確認してください。
- 必要に応じて、各タイプのSVG表現やキーワードを教材内容に合わせて追加してください。

## チャッピーに相談すべき点

- `comparison`、`flow`、`relation` の図形表現を、Up塾の教材デザインとしてどこまで具体化するか相談するとよいです。
